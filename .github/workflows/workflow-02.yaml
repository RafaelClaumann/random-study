name: using-variables-workflow

env:
  USER_NAME: "rafael"
  USER_PASSWORD: "1234"

on:
  push:
    paths: 
      - ".github/workflows/workflow-02.yaml"

jobs:
  using_workflow_level_variables:
    runs-on: ubuntu-latest
    steps:
      - name: Using Workflow Variables
        run: printf "{ $USER_NAME } and { $USER_PASSWORD }\n"
