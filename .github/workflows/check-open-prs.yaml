name: Github API Calls

on:
  - workflow_dispatch

jobs:
  comment:
   runs-on: ubuntu-latest
   steps:
    - uses: actions/github-script@v6
      id: my-script
      with:
        result-encoding: string
        retries: 3
        script: |
          github.rest.branches({
            owner: context.repo.owner,
            repo: context.repo.repo,
          })
