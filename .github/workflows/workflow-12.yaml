name: Calling Reusable Workflow

on:
  - workflow_dispatch

jobs:
  call-workflow-1-in-local-repo:
    uses: rafaelclaumann/random-study/.github/workflows/workflow-reusable-11.yaml@f9234a234a459811887bcc28f73eff53bf6ebe0a
    with:
      config-path: CALL_REUSABLE_WORKFLOW_WITH_COMMIT_SHA

  call-workflow-2-in-local-repo:
    uses: rafaelclaumann/random-study/.github/workflows/workflow-reusable-11.yaml@main
    with:
      config-path: CALL_REUSABLE_WORKFLOW_WITH_BRANCH_NAME

  call-workflow-3-in-local-repo:
    uses: ./.github/workflows/workflow-reusable-11.yaml
    with:
      config-path: CALL_REUSABLE_WORKFLOW_WITH_BRANCH_NAME      
