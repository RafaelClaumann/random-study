services:
  demoapp:
    image: demoapp:1.0
    container_name: app
    ports:
      - "8080:8080"

  prometheus:
    image: prom/prometheus
    container_name: prometheus
    volumes:
      - ./prometheus-settings.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"
